import { Visibility as MUIVisibilityIcon } from '@mui/icons-material'
import { styled } from '@mui/material/styles';

const VisibilityIcon = styled(MUIVisibilityIcon)({
  
});

export default VisibilityIcon;