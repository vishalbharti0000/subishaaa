import Card from "../../atoms/Card/Card";
import { Link } from "@mui/material";
import styled from "@emotion/styled";

export const RegistrationStyle = styled(Card)`
    margin: 50px auto;
    opacity: 0.95;
`

export const LinkStyle = styled(Link)`
    text-decoration: none;
    line-height: 10px;
`